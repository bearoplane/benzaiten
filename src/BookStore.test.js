import BookStore from './BookStore';

it('accepts an array of books', () => { });

it('accepts a single book', () => { });

it('reject a malformed book', () => { });

it('sets the id of a book based on the item_barcode', () => { });

it('can toggle a book selected or not', () => { });

it('returns all books', () => { });

it('returns a single book', () => { });

it('returns all selected books', () => { });
